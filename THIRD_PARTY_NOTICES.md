# Third-party notices

## DeepSeek Harness

This plugin builds on the
[DeepSeek Harness](https://github.com/deepseek-ai/deepseek-harness) plugin
ecosystem (`@deepseek-ai/dsh-*` packages). It calls the harness runtime
services (`ctx.llm`, `ctx.storageDomain`, `ctx.webServer`, `ctx.connection`,
`ctx.agentDefaultModel`) and reuses the browser-side design tokens
(`--dsw-alias-*`) and the `MarkdownText` primitive from
`@deepseek-ai/dsh-client-ui-primitives`.

Copyright (c) 2026 DeepSeek

DeepSeek Harness is distributed under the MIT License. The full terms are
reproduced in `LICENSES/DeepSeek-Harness-MIT.txt`.
